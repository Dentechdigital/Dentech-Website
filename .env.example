# Copy to .env for local Netlify Functions (used by `npx netlify dev`).
# Never commit real keys.

# Required for live Chat tab (server-side only; Netlify function calls Gemini).
GEMINI_API_KEY=

# Optional; default in code is gemini-2.0-flash
# GEMINI_MODEL=gemini-2.0-flash
