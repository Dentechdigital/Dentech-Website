import { SITE_ORIGIN } from './aboutContent';

/** Shown in static index.html (Google often crawls this before JS). Keep in sync with Home SEO. */
export const DEFAULT_DOCUMENT_TITLE = 'Dentech Digital | Ottawa Dental Marketing Agency';

export const DEFAULT_META_DESCRIPTION =
  'Ottawa dental marketing agency for Canadian clinics: local SEO, GEO, high-converting websites, paid media, and reputation systems that drive qualified bookings.';

export const DEFAULT_OG_IMAGE = `${SITE_ORIGIN}/dentist-cutout-800w.webp`;
