/**
 * Scarcity copy — capacity- and quality-framed (not countdown / stock gimmicks).
 * Update cohort size or window here if the business changes the offer.
 */
export const SCARCITY_COHORT_MAX = 10;

export const scarcityShortLabel = 'Limited enrollment';

export const scarcityHeroSupporting = `Limited enrollment: up to ${SCARCITY_COHORT_MAX} qualifying practices this window — we protect launch quality with capped onboarding. Submitting the form does not reserve a slot; we confirm fit on your call.`;

export const scarcityOfferCallout = `Up to ${SCARCITY_COHORT_MAX} qualifying practices in the current window. Application does not guarantee a slot — we confirm fit and availability on your call.`;
