/**
 * Scarcity copy — capacity- and quality-framed (not countdown / stock gimmicks).
 * Update cohort size or window here if the business changes the offer.
 */
export const SCARCITY_COHORT_MAX = 10;

export const scarcityShortLabel = 'Limited enrollment';

export const scarcityHeroSupporting = `This website-included package is offered for a limited time and with capped onboarding so we can protect launch timelines and creative quality. We are currently accepting up to ${SCARCITY_COHORT_MAX} qualifying practices under this program — once that cohort is full, next openings are reviewed individually.`;

/** One line for the apply section — reinforces capacity without repeating the hero. */
export const scarcityApplyNote = `Limited enrollment: up to ${SCARCITY_COHORT_MAX} qualifying practices in the current window. We confirm fit and availability after we review your request.`;

export const scarcityOfferCallout = `We are enrolling up to ${SCARCITY_COHORT_MAX} qualifying practices under this bundled offer in the current window. Submitting an application does not guarantee a slot; we confirm fit and availability on your call.`;
